/**
 * useMessagesStore — legacy stub.
 * Replaced by useChatStore for FR03.
 * Kept to avoid unused-import errors in any future migration.
 */
export { useChatStore as useMessagesStore } from './useChatStore'
